{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Expo (mobile)",
      "type": "node-terminal",
      "request": "launch",
      "command": "npx expo start --dev-client"
    }
  ]
}
